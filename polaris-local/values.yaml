# Polaris only – no auth, no ingress. Use port-forward to test.
replicaCount: 1

image:
  repository: apache/polaris
  tag: "1.3.0-incubating"

persistence:
  type: in-memory

ingress:
  enabled: false

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 1Gi
